---
id: installation
description: How to install Swirl
---

import Tabs from "@theme/Tabs";
import TabItem from "@theme/TabItem";

# Installation

Best way to get Swirl currently is to build it from source.

## Prerequisites

To build Swirl from source, you need the following prerequisites:
- Git
- C++ compiler (e.g., `g++` or `clang++`)
    - Make sure it supports C++23
- CMake (version 3.20 or higher)
- LLVM Development libraries (version 17 or higher)

## Linux
<Tabs groupId="os">
<TabItem value="Ubuntu" label="Ubuntu">
Update packages:
```bash
sudo apt update && sudo apt upgrade -y
```
Install the required packages:
```bash
sudo apt install git cmake clang llvm-dev
```
</TabItem>

<TabItem value="fedora" label="Fedora">
Update packages:
```bash
sudo dnf update -y
```
Install the required packages:
```bash
sudo dnf install git cmake clang llvm-devel
```
</TabItem>
<TabItem value="arch" label="Arch Linux">
Update packages:
```bash
sudo pacman -Syu
```
Install the required packages:
```bash
sudo pacman -S git cmake clang llvm
```
</TabItem>
</Tabs>

## MacOS
Install the required packages:
```bash
brew install git cmake llvm
```

After installing LLVM you need to add LLVM binaries and libraries to your PATH.
<Tabs groupId="macos">
<TabItem value="zsh" label="Zsh">
```bash
echo 'export PATH="/usr/local/opt/llvm/bin:$PATH"' >> ~/.zshrc && \
echo 'export LLVM_DIR="/usr/local/opt/llvm/lib/cmake/llvm"' >> ~/.zshrc
```
```bash
source ~/.zshrc
```
</TabItem>
<TabItem value="bash" label="Bash">
```bash
echo 'export PATH="/usr/local/opt/llvm/bin:$PATH"' >> ~/.bashrc && \
echo 'export LLVM_DIR="/usr/local/opt/llvm/lib/cmake/llvm"' >> ~/.bashrc
```
```bash
source ~/.bashrc
```
</TabItem>
<TabItem value="fish" label="Fish">
```bash
fish_add_path /usr/local/opt/llvm/bin &&
set -Ux LLVM_DIR /usr/local/opt/llvm/lib/cmake/llvm
```
</TabItem>
</Tabs>

## Windows

- First download MSYS from [msys2.org](https://www.msys2.org/).

- Open the CLANG64 shell.

- Update packages:
    ```bash
    pacman -Syu
    ```

- Install the required packages:
    ```bash
    pacman -S mingw-w64-clang-x86_64-cmake mingw-w64-clang-x86_64-toolchain
    ```
        - It will install cmake, clang, llvm dev libs, git, make etc.

- Set up the environment variables:
    Add the following line to your path environment variable in windows settings
    ```
    C:\msys64\clang64\bin
    ```
    [Here's](https://www.computerhope.com/issues/ch000549.htm#windows11) a guide for that.

You're system should now be set up to build Swirl.

## Building
Clone the repository and navigate into it:
```bash
git clone https://github.com/swirllang/swirl.git
```
```bash
cd swirl
```

Generate the build files:
```bash
cmake -B build -S .
```

Build the project:
```bash
cmake --build build --config Release
```
The Swirl binary will be located in the `build` directory.

## Running
To run Swirl, you can use the following command:
```bash
./build/swirl
```

## Dev setup
To get the best experience building Swirl, we recommend using [Visual Studio Code](https://code.visualstudio.com/) with the following extensions:
- [Swirl Extension](https://marketplace.visualstudio.com/items?itemName=mrinmoyhaloi.swirl-lang-support)
- [C/C++ by Microsoft](https://marketplace.visualstudio.com/items?itemName=ms-vscode.cpptools) or [clangd by LLVM](https://marketplace.visualstudio.com/items?itemName=llvm-vs-code-extensions.vscode-clangd)
- [CMake Tools](https://marketplace.visualstudio.com/items?itemName=ms-vscode.cmake-tools)
